# ---------- Common ----------
ASPNETCORE_ENVIRONMENT=Development
# Ex: http://localhost:8080 (local) or http://api:8080 (inside containers)
API_BASE_URL=http://localhost:8080
WEB_BASE_URL=http://localhost:8081

# ---------- DB (dev simple = SQLite; team/prod = Postgres) ----------
# If POSTGRESQL is set/valid, server will use Npgsql; otherwise falls back to SQLite.
# Example: Host=postgres;Port=5432;Database=keycard;Username=keycard;Password=keycard
POSTGRES_CONNECTION=

# ---------- Postgres container defaults (used by compose) ----------
POSTGRES_USER=keycard
POSTGRES_PASSWORD=keycard
POSTGRES_DB=keycard

# ---------- Redis (optional but provisioned by compose) ----------
REDIS_CONNECTION=redis:6379

# ---------- CORS ----------
# Comma-separated origins allowed for Web & Desktop during dev.
CORS_ALLOWED_ORIGINS=http://localhost:8081,http://127.0.0.1:8081
